{
  "name": "SyncState",
  "type": "object",
  "properties": {
    "key": {
      "type": "string",
      "description": "Identifier for which sync state this is (e.g. 'googlecalendar')"
    },
    "sync_token": {
      "type": "string",
      "description": "Google Calendar incremental sync token"
    }
  },
  "required": [
    "key"
  ]
}